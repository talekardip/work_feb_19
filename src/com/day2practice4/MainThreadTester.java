package com.day2practice4;


import com.training.day2.RunnableThread;

public class MainThreadTester {
    public static void main(String[] args) throws InterruptedException {
        //m1
        /*
        TableThread r1 = new TableThread("Thread-1",5);   //instance is created
        Thread t1 = new Thread(r1);     // now thread is created
        t1.start();
        t1.join();

        TableThread r2 = new TableThread("Thread-2",10);   //instance is created
        Thread t2 = new Thread(r2);     // now thread is created
        t2.start();

         */
        /*
        //m2 by using synchronization
        Table table = new Table();
        Thread1 t1 = new Thread1(table);
        //t1.setDaemon(true);
        System.out.println("Thread 1 priority "+t1.getPriority());
        Thread2 t2 = new Thread2(table);
        //set priority
        //t2.setPriority(10);
        t1.start();
        t2.start();
        */




    }
}
